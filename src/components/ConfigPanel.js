import { View } from "react-native";
import { RegularText } from "../common/Text";

export default function ConfigPanel() {
  return (
    <View>
      <RegularText>config panel</RegularText>
    </View>
  );
}
